.history  
  -if @served_quizzes.blank?
    %table.table-bordered{style: "margin-top:15px;width:100%;"}
      %tbody
        %tr 
          %td There is nothing to show in history
  -else
    %table.table-bordered{style: "width:100%;margin-bottom:15px;"}
      %thead
        %th Quiz Title
        %th Students
        %th Served Date
        %th Status
      %tbody
        -@served_quizzes.each do |served_quiz|
          %tr
            %td 
              = served_quiz.quiz_bank.title
            %td
              = "Students: #{served_quiz.sharings.count}"  
            %td
              = "#{time_ago_in_words(served_quiz.created_at)} ago" 
            %td
              = "implemented later"             