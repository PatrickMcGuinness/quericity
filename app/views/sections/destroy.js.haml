$('#base-modal').modal('hide');
$(".section##{@section.id}").remove()
$(".questions##{@section.id}").remove()
if($(".section").length == 0){
$(".sections").append("<div class = 'top_bar_s no-record'>There are no sections.</div>")
}